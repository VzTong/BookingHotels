@model IEnumerable<App.Data.Entities.Hotel.AppBranchHotel>
@{
	int? selectedId = (int?)ViewBag.SelectedId;
}
@foreach (var item in Model)
{
	if (selectedId.HasValue && selectedId.Value == item.Id)
	{
		<option selected="selected" value="@item.Id">@item.Name - @item.Address</option>
		continue;
	}
	<option value="@item.Id">@item.Name - @item.Address</option>

}